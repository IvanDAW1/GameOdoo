<odoo>
    <data>

        <record model="ir.actions.act_window" id="game.action_player">
            <field name="name">Players</field>

            <field name="res_model">game.player</field>
            <field name="view_mode">tree,form,kanban</field>
        </record>

        <!-- Top menu item -->

        <menuitem name="game" id="game.menu_root"/>

        <!-- menu categories -->

        <menuitem name="Menu" id="game.menu_1" parent="game.menu_root"/>

        <!-- actions -->

        <menuitem name="Player" id="game.menu_1_player" parent="game.menu_1" action="game.action_player"/>

    </data>
</odoo>